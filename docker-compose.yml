services:
  movilizaciones:
    build: .
    container_name: asignacion_movilizaciones
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
